#!/bin/bash

set -e
shopt -s dotglob
echo "---> Installing application source ..."
mv /tmp/src/* ./
pip install -U pip
echo "---> Installing application ..."
echo $OPENSHIFT_BUILD_COMMIT > $COMMIT_FILE
pip install .
# set permissions for any installed artifacts
fix-permissions /opt/app-root 2>/dev/null
